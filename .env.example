# Supabase project (https://app.supabase.com)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only, used for admin actions (approval, token generation, export)
# NEVER expose this to the client / commit real values.
SUPABASE_SERVICE_ROLE_KEY=

# Random secret used to sign/verify one-time voting tokens
VOTE_TOKEN_SECRET=
